package com.forrester.research;

public class LogErrorMessages {

    public static final String INVALID_IPTYPE = "Invalid Ip type to determine access. Currently supported values are (case insensitive): ";

    private LogErrorMessages() throws IllegalAccessException {
        throw new IllegalAccessException("For static use only");
    }
}
