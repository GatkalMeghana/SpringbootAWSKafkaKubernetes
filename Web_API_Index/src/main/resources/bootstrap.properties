################################
# Basic Configuration
################################
spring.application.name=index-service
spring.profiles.active=${profile}
spring.jackson.serialization.indent_output=true
################################
# Config Server Configuration
################################
spring.cloud.config.uri=${configURI}
spring.cloud.config.username=${configUser}
spring.cloud.config.password=${configPass}
encrypt.key=${propKey}
################################
# Server Configuration
################################
server.servlet.context-path=/ForrSVC
server.port=9046
server.tomcat.basedir=${app.home}/tomcat
server.tomcat.accesslog.enabled=true
server.error.include-message=always
################################
# Management Configuration
################################
management.endpoints.web.exposure.include=health,info,env,shutdown,loggers,refresh,caches
management.endpoint.shutdown.enabled=true
endpoints.shutdown.enabled=true
management.endpoint.health.show-details=always
management.health.elasticsearch.response-timeout=1s
spring.elasticsearch.rest.uris=${forr.elasticsearch.domain.url}
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.elasticsearch.ReactiveElasticsearchRestClientAutoConfiguration

################################
# JPA / Hibernate configuration
################################
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=none
#spring.jpa.database-platform=org.hibernate.dialect.Oracle10gDialect   (auto-detected by default)
#IMPORTANT
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.datasource.tomcat.max-wait=20000
spring.datasource.tomcat.max-active=50
spring.datasource.tomcat.max-idle=20
spring.datasource.tomcat.min-idle=15

#Taxonomy Configuration
forr.taxonomies.url.page=1
forr.taxonomies.url.size=100000

forr.taxonomy.methodology.path=Legacy,Old Forrester Product,Data
forr.taxonomy.methodology.path.seperator=,
forr.taxonomy.methodology.values=Consumer Technographics,Business Technographics

forr.company.interview.expression=(?i)companies interviewed.*